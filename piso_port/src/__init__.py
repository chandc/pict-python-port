"""Shared foundation for the educational PISO port: grid metrics, operators, and solvers.

Scripts at the repository root (test_*, plot_*, run_*, nn_*) import from here; nothing in this
package imports from them, so the dependency direction stays one-way.
"""
